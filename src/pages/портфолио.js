import React from "react";
import {Link} from 'gatsby';

const PortfolioPage = () => {
    return (
        <div>
            <h1>Portfolio</h1>
            <Link to={'/'}>Назад</Link>
        </div>
    );
};

export default PortfolioPage;
